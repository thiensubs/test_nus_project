<%= form_for([@post, @post.comments.build], :remote => true) do |f| %>

 <p>
    <%= f.label :title_comment %><br />
    <div class="form-group">
    <%= f.text_field :title_comment, class: "form-control" %>
  </div>

  </p>
  <p>
    <%= f.label :content_comment %><br />
    <%= f.text_area :content_comment, class: "form-control", rows: "3" %>
  </p>

  <div class="actions">
    <%= f.submit "Create Comment", class: "btn btn-large btn-primary"  %>
  </div>
<% end %>
